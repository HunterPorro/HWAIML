This folder holds older experiments. The clean submission path is:

  python3 optimize_to_810k.py

Alternate in the project root: optimize_profit30.py.

The only remaining optimize_profit* script here is optimize_profit34.py (probabilistic
offer variant). It uses the same inputs as the main pipeline (Cars_HW_data.csv +
Cars_HW_template.xlsx) only.

Also here: check_test_price.py, and Used_Car_Pricing.ipynb.
